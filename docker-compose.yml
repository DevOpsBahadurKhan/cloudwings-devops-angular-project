version: '3.8'

services:
  web:
    build: .
    container_name: web
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - my_network

networks:
  my_network:
    driver: bridge
